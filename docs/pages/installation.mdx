# Installation

## Installing with **npm**

For more information on using `npm` check out the docs [here](https://docs.npmjs.com/cli/install)

```bash copy
npm install @bitfiber/rx
```

## Installing with **yarn**

For more information on using `yarn` check out the docs [here](https://yarnpkg.com/getting-started/usage#installing-all-the-dependencies)

```bash copy
yarn add @bitfiber/rx
```
