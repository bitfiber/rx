import {Badge} from '../../../../';

# set <Badge type={'method'}>Method</Badge>

Sets a new value

`@param value: T {:ts}`  
A new form value

## API

```ts copy
set(value: T): void;
```

## Example

```ts copy
import {cookiePart} from '@bitfiber/rx';

const ckPart = cookiePart('key');
ckPart.set({value: 'value'});
```
