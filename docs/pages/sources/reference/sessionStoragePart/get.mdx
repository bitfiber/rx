import {Badge} from '../../../../';

# get <Badge type={'method'}>Method</Badge>

Retrieves the current value

`@returns T {:ts}`

## API

```ts copy
get(): T;
```

## Example

```ts copy
import {sessionStoragePart} from '@bitfiber/rx';

const ssPart = sessionStoragePart('key');
const value = ssPart.get();
```
